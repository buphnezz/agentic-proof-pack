apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-proof.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: { app: {{ include "agentic-proof.fullname" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
