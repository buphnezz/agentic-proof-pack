apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentic-proof.fullname" . }}-secret
type: Opaque
stringData:
  AUDIT_SIGNING_KEY: "{{ .Values.secrets.AUDIT_SIGNING_KEY }}"
  JWT_SECRET: "{{ .Values.secrets.JWT_SECRET }}"
  API_KEYS: "{{ .Values.secrets.API_KEYS }}"
